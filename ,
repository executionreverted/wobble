import React from 'react';
import {
  View,
  Text,
  StyleSheet
} from 'react-native';
import { Room } from '../../types';

interface RoomHeaderProps {
  room: Room;
  onBackPress: () => void;
}

const RoomHeader: React.FC<RoomHeaderProps> = ({ room }) => {
  return (
    <View style={styles.container}>
      <View style={styles.titleContainer}>
        <Text style={styles.title}>#{room.name}</Text>
        <Text style={styles.description}>{room.description}</Text>
      </View>
    </View>
  );
};

const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    alignItems: 'center',
    backgroundColor: '#36393f',
    borderBottomWidth: 1,
    borderBottomColor: '#2f3136',
    padding: 16,
    paddingTop: 40, // Extra padding to accommodate the menu buttons
    height: 80,
  },
  titleContainer: {
    flex: 1,
    marginLeft: 40, // Leave space for the menu button
  },
  title: {
    color: '#fff',
    fontSize: 18,
    fontWeight: 'bold',
  },
  description: {
    color: '#b9bbbe',
    fontSize: 14,
  },
});

export default RoomHeader;
